[package]
name = "xtask"
version = "0.0.0"
edition = "2021"
authors = ["Ross Younger <qcp@crazyscot.com>"]
license = "MIT" # Note this license applies only to xtask, not to qcp itself.
publish = false

[dependencies]
anyhow = { workspace = true }
cargo_toml = "0.21.0"
chrono = { version = "0.4.39", default-features = false, features = ["now"] }
flate2 = "1.0.35"
pico-args = "0.5.0"
xshell = "0.2.7"
